<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/collapsable"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/activity_horizontal_margin"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/card_player"
        android:layout_alignParentTop="true"
        android:layout_width="fill_parent"
        android:layout_height="220dp"
        android:layout_gravity="center"
        android:gravity="center_horizontal"
        android:layout_marginBottom="@dimen/padding_small"
        android:visibility="visible"/>

    <Button
        android:id="@+id/btn_jumble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/card_player"
        style="@style/button"
        android:background="@android:color/transparent"
        android:textColor="@color/storymaker_highlight"
        android:drawablePadding="@dimen/padding_small"
        android:paddingTop="@dimen/padding_xsmall"
        android:text="Jumble"
        android:visibility="gone"/>

    <Button
        android:id="@+id/btn_order"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/button"
        android:layout_below="@id/btn_jumble"
        android:background="@android:color/transparent"
        android:textColor="@color/storymaker_highlight"
        android:drawablePadding="@dimen/padding_small"
        android:paddingTop="@dimen/padding_xsmall"
        android:text="Order" />

    <Button
        android:id="@+id/btn_narrate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/button"
        android:layout_below="@id/btn_order"
        android:background="@android:color/transparent"
        android:textColor="@color/storymaker_highlight"
        android:drawablePadding="@dimen/padding_small"
        android:paddingTop="@dimen/padding_xsmall"
        android:text="Narrate" />

    <Button
        android:id="@+id/btn_publish"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/button"
        android:layout_below="@id/btn_narrate"
        android:background="@android:color/transparent"
        android:textColor="@color/storymaker_highlight"
        android:drawablePadding="@dimen/padding_small"
        android:paddingTop="@dimen/padding_xsmall"
        android:text="Publish" />
</RelativeLayout>
